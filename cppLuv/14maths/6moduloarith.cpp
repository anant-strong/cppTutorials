//modulo arithmetics or fast power
#include <bits/stdc++.h>
using namespace std;
int main()
{
    // (a+b)%m = a%m + b%m;

    // (a-b)%m = (a%m - b%m + m)%m;

    // (a*b)%m = a%m*b%m;

    // (a%m)%m%m..... = a%m; 
    return 0;
}